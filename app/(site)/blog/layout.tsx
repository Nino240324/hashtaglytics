export const metadata = {
  title: 'Blog — Hashtaglytics',
  description:
    "Ce que nous mesurons sur le référencement local français. Données originales, échantillons indiqués, méthode publiée.",
};

export default function BlogLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}