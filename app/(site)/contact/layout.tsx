export const metadata = {
  title: 'Contact — Hashtaglytics',
  description:
    'Une question sur le service, les données ou votre compte ? Écrivez-nous.',
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}